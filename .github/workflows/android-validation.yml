name: Android validation

on:
  push:
    branches:
      - agent/fix-core-gameplay-invariants
  pull_request:

concurrency:
  group: save-integrity-remediation-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  validate:
    name: Apply save integrity and validate debug/release
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Check out remediation branch
        uses: actions/checkout@v4

      - name: Apply save integrity wiring
        shell: bash
        run: |
          python3 scripts/patch_save_integrity_existing.py
          python3 scripts/patch_save_integrity_followups.py

      - name: Verify save integrity contracts
        shell: bash
        run: |
          test -f app/src/main/java/com/anurag9000/forestrun/engine/SaveIntegrityManager.kt
          test -f app/src/test/java/com/anurag9000/forestrun/engine/SaveIntegrityManagerTest.kt
          grep -q 'SaveIntegrityManager.repair(this)' app/src/main/java/com/anurag9000/forestrun/MainActivity.kt
          grep -q 'useCompatibilityPreferences' app/src/main/java/com/anurag9000/forestrun/engine/SaveManager.kt
          grep -q 'activePrefsNameForTests' app/src/main/java/com/anurag9000/forestrun/engine/SaveManager.kt
          grep -q 'activeGhostFilenameForTests' app/src/main/java/com/anurag9000/forestrun/engine/SaveManager.kt
          grep -q 'context.getSharedPreferences(activePrefsName' app/src/main/java/com/anurag9000/forestrun/engine/SaveManager.kt
          grep -q 'File(context.filesDir, activeGhostFilename)' app/src/main/java/com/anurag9000/forestrun/engine/SaveManager.kt
          grep -q 'requiredLastRunKeys.any' app/src/main/java/com/anurag9000/forestrun/engine/SaveIntegrityManager.kt
          grep -q 'distanceM.takeIf { it.isFinite() }' app/src/main/java/com/anurag9000/forestrun/engine/SaveManager.kt
          grep -q 'current == Int.MAX_VALUE' app/src/main/java/com/anurag9000/forestrun/engine/SaveManager.kt
          grep -q 'val all = prefs.all' app/src/main/java/com/anurag9000/forestrun/engine/FeedbackSettings.kt
          grep -q 'future schema is preserved without destructive downgrade' app/src/test/java/com/anurag9000/forestrun/engine/SaveIntegrityManagerTest.kt
          grep -q 'partial last run summary is discarded' app/src/test/java/com/anurag9000/forestrun/engine/SaveIntegrityManagerTest.kt

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: gradle

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android API 36
        shell: bash
        run: |
          set +o pipefail
          yes | sdkmanager 'platforms;android-36' >/dev/null
          status=${PIPESTATUS[1]}
          set -o pipefail
          exit "$status"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Validate Gradle wrapper
        uses: gradle/actions/wrapper-validation@v4

      - name: Verify accumulated release contracts
        shell: bash
        run: |
          grep -q 'applicationId = "com.anurag9000.forestrun"' app/build.gradle.kts
          grep -q 'targetSdk = 36' app/build.gradle.kts
          grep -q 'safeContentTransform.toLogical' app/src/main/java/com/anurag9000/forestrun/engine/GameView.kt
          grep -q 'GhostPersistenceManager.saveBestRunAsync' app/src/main/java/com/anurag9000/forestrun/engine/GameView.kt
          grep -q 'AtomicFile' app/src/main/java/com/anurag9000/forestrun/engine/SaveManager.kt
          grep -q 'FeedbackSettings.hapticsEnabled' app/src/main/java/com/anurag9000/forestrun/engine/HapticManager.kt
          grep -q 'FeedbackSettings.audioEnabled' app/src/main/java/com/anurag9000/forestrun/engine/SfxManager.kt
          grep -q 'FeedbackSettings.reducedMotion' app/src/main/java/com/anurag9000/forestrun/engine/CameraSystem.kt
          grep -q 'private val tempoEvaluationThrottle' app/src/main/java/com/anurag9000/forestrun/engine/LeitmotifManager.kt
          if git grep -n -E 'com\.google\.gson|Gson|SerializedName|TypeToken' -- app/src; then
            exit 1
          fi
          if grep -Eq -- '^-keep[[:space:]]+class[[:space:]]+com\.anurag9000\.forestrun\.\*\*' app/proguard-rules.pro; then
            exit 1
          fi

      - name: Compile debug and release sources
        shell: bash
        run: |
          set +e
          ./gradlew compileDebugKotlin compileDebugUnitTestKotlin compileReleaseKotlin \
            --no-daemon --stacktrace --console=plain > compile.log 2>&1
          status=$?
          if [[ $status -ne 0 ]]; then
            echo '::group::Kotlin compiler diagnostics'
            grep -E '(^e: | error: |FAILURE:|Execution failed for task|\* What went wrong:)' compile.log || true
            echo '::endgroup::'
            tail -n 320 compile.log
          fi
          exit $status

      - name: Run full unit suite
        run: ./gradlew testDebugUnitTest --no-daemon --stacktrace --console=plain

      - name: Run debug and release lint
        run: ./gradlew lintDebug lintRelease --no-daemon --stacktrace --console=plain

      - name: Assemble debug and instrumentation APKs
        run: ./gradlew assembleDebug assembleDebugAndroidTest --no-daemon --stacktrace --console=plain

      - name: Build minified release bundle
        run: ./gradlew bundleRelease --no-daemon --stacktrace --console=plain

      - name: Verify artifacts and effective shrinking
        shell: bash
        run: |
          test -s app/build/outputs/apk/debug/app-debug.apk
          test -s app/build/outputs/apk/androidTest/debug/app-debug-androidTest.apk
          test -s app/build/outputs/bundle/release/app-release.aab
          test -s app/build/outputs/mapping/release/mapping.txt
          python3 - <<'PY'
          from pathlib import Path
          mapping = Path('app/build/outputs/mapping/release/mapping.txt')
          for raw in mapping.read_text(encoding='utf-8').splitlines():
              line = raw.strip()
              if line.startswith('com.anurag9000.forestrun.') and line.endswith(':') and ' -> ' in line:
                  original, renamed = line[:-1].split(' -> ', 1)
                  if original != renamed:
                      break
          else:
              raise SystemExit('R8 mapping contains no obfuscated Forest Run classes')
          PY

      - name: Upload diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: save-integrity-remediation-diagnostics
          path: |
            compile.log
            app/build/reports/tests/testDebugUnitTest/
            app/build/test-results/testDebugUnitTest/
            app/build/reports/lint-results-debug.html
            app/build/reports/lint-results-release.html
            app/build/outputs/mapping/release/mapping.txt
          if-no-files-found: warn
          retention-days: 7
