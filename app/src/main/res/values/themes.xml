<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Full-screen immersive game theme for the API 36 application target.
        Status and navigation bars are hidden in code; essential UI is mapped
        into system-bar and display-cutout safe content bounds.
    -->
    <style name="Theme.ForestRun" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>
</resources>
