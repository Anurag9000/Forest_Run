<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Full-screen immersive game theme.
        Uses Theme.AppCompat.NoActionBar as parent (stable in compileSdk 34).
        Status bar / nav bar are hidden in code via immersive sticky flags.
    -->
    <style name="Theme.ForestRun" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>
</resources>