# Forest Run — Pointer Input Admission Audit

Date: 2026-08-02  
Repository: `Anurag9000/Forest_Run`  
Canonical branch: `main`

## Completed scope

This continuation closed the isolated `MainMenuScreen.onTap()` malformed-coordinate debt without changing menu artwork, copy, layout, or finite-input behavior.

Implemented directly on `main`:

1. `FiniteCoordinateAdmission.kt` — shared finite-coordinate predicate.
2. `FiniteCoordinateAdmissionTest.kt` — exhaustive finite and nonfinite matrix.
3. `MainMenuScreen.kt` — predicate is the first executable `onTap()` statement.
4. `MainMenuScreenInputTest.kt` — nonfinite state-mutation rejection plus finite Garden and ritual behavior.
5. `test_main_menu_input_contract.py` — balanced source extraction and guard-order contract.
6. `FeedbackSettingsPanel.kt` — existing nonfinite hit-test check consolidated onto the shared predicate.
7. `FINITE_POINTER_INPUT_ADMISSION.md` — canonical operator/developer contract.

## Exact preservation evidence

The commit that integrated the menu guard changed `MainMenuScreen.kt` by exactly one added line and zero deletions. The settings-panel consolidation changed one line by one addition and one deletion. No visual drawing code, authored copy, sprite selection, layout geometry, or phase behavior was otherwise modified.

## Validation performed

- The pure Kotlin admission matrix executed successfully with `kotlinc` and Java.
- The balanced Python source-order contract syntax-compiled and all three checks passed against the exact current `onTap()` method region.
- Existing checked-in settings tests already cover nonfinite taps, malformed surfaces, and mutated rectangles.
- A Robolectric menu integration contract was added but was not executed through the unavailable exact-head Gradle environment.

No complete exact-head Android unit, Robolectric, lint, release-build, package, connected-emulator, or physical-device result is claimed.

## Debt removed

The prior item "add finite-coordinate admission inside `MainMenuScreen.onTap()`" is complete.

## Remaining bounded runtime debt

1. integrate `FamiliarityWarmthScoring` into `RelationshipArcSystem`;
2. harden coordinator-level finite delta and speed admission in `ParallaxBackground.update()`;
3. harden the public frame boundary in `GameView.update()`;
4. decompose `GameView` and consolidate distributed persistence ownership through behavior-preserving seams.

`ParallaxLayer.update()` is already fail-closed for malformed or reversing inputs. The remaining parallax issue concerns coordinator-owned ambience, Bloom blending, and current-speed state before the layer boundary.

## Classification

This change removes one runtime state-corruption path but does not complete release validation. Forest Run remains a feature-rich alpha until exact-head execution, physical acceptance, signing, delivery, visual, accessibility, and policy gates are complete.
